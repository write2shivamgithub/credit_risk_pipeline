from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A small pipeline project related to credit risk and the dataset we are working on a bandhan bank',
    author='shivam',
    license='',
)
